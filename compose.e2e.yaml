services:
  e2e-test-runner:
    build: ./e2e
    environment:
      - DIFFICALCY_HOST=difficalcy:80
      - DIFFICALCY_FULL_HOST=difficalcy-full:80
      - UPDATE_SNAPSHOTS
    volumes:
      - ./e2e/snapshot.txt:/e2e/snapshot.txt
    depends_on:
      - difficalcy
      - difficalcy-full

  beatmap-server:
    image: caddy:2-alpine
    volumes:
      - ./e2e/beatmaps:/usr/share/caddy:ro

  cache:
    image: redis:8
    volumes:
      - redis-data:/data

  difficalcy:
    build:
      context: .
      target: publish
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - REDIS_CONFIGURATION=cache:6379
      - BEATMAP_DOWNLOAD_URL=http://beatmap-server:80/{beatmapId}.osu
    volumes:
      - beatmaps:/beatmaps
    depends_on:
      - beatmap-server
      - cache

  difficalcy-full:
    build:
      context: .
      target: publish-full
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - REDIS_CONFIGURATION=cache:6379
      - BEATMAP_DOWNLOAD_URL=http://beatmap-server:80/{beatmapId}.osu
    volumes:
      - beatmaps:/beatmaps
    depends_on:
      - beatmap-server
      - cache

volumes:
  beatmaps:
  redis-data:
